#!/bin/sh
# Placeholder until the server suite lands (plan WP4): starts nothing, runs
# nothing, exits 0 so `pixi run test` is the unit suite alone for now.
echo "== server suite: not yet implemented"
